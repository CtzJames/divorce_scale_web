---
name: "量表网页端"
description: "面向婚姻冲突与离婚决策场景的专业自测与支持分流工具"
---

# Design System: 量表网页端

## 1. Overview

**Creative North Star: “安定的专业会谈入口”**

本设计系统服务于两套婚姻关系风险测评工具：离婚力量表，以及配偶高自恋特质与高冲突婚姻风险自测量表。界面的底层是 product interface，核心任务是帮助用户完成测评、理解结果、选择后续支持、提交可靠联系信息。表层应呈现专业服务质感和中性合作平台感，而不是单一律所官网、心理诊断页面或营销落地页。

整体视觉方向是克制、精致、可信的专业咨询工具风格。页面应让用户愿意停留、愿意相信、能读懂结果、愿意提交真实联系方式，并且在敏感议题中不会被进一步刺激。面向高价值咨询场景的成熟质感应来自留白、比例、排版、细腻色彩、清晰层级和低噪音交互，而不是奢华、炫耀或强销售。

设计必须遵守三个边界：第一，测评结果不是心理诊断、NPD 诊断或法律意见；第二，法律支持、心理支持与深度报告应并列呈现，服务意向由用户主动选择；第三，首批 UI 优化应优先服务 `/narcissism-risk` 结果页，不破坏计分、写库、`service_intent`、表单校验、PNG 导出和旧离婚力量表稳定链路。

**Key Characteristics:**

- 克制、可信、清晰、安定、温和、精致；
- 中性合作平台感，而非单一机构导流；
- 低噪音视觉环境，避免焦虑营销；
- 结构化结果表达，先理解结果，再选择行动；
- 移动端可读、可点、可提交；
- 后续完整视觉审查必须基于真实页面截图、localhost、本地浏览器或等价视觉证据。

## 2. Colors

色彩系统必须区分“品牌界面色”和“风险语义色”。品牌界面色用于背景、标题、按钮、卡片、表单和整体气质；风险语义色只用于结果等级、风险标签、安全提示与图表状态。两套颜色不得混用，避免用户将页面主色误解为风险等级含义。

### Primary

- **雾蓝灰 / 石墨蓝方向**：用于主按钮、关键标题、导航强调、重要但非风险语义的界面元素。它应显得稳定、可信、专业，不应走纯蓝法律模板，也不应使用高饱和科技蓝。

### Secondary

- **暖灰褐 / taupe 方向**：用于辅助强调、轻量分隔、说明性区域和视觉呼吸。它应提供成熟咨询感，而不是咖啡馆、奢侈品或会所感。
- **浅灰绿环境辅助色**：只能轻量用于背景或非语义辅助装饰。不要把绿色作为全站主色或主要按钮色，以免与“低风险 / 安全 / 通过”的结果语义混淆。

### Neutral

- **暖白底**：页面背景应使用带轻微温度的 near-white，而不是刺眼纯白。
- **柔和中性色**：正文、边框、分隔、卡片背景应使用低噪音中性色。避免纯黑、纯白和无倾向灰色导致的生硬感。

### Risk Semantic Colors

风险等级色应独立建立低饱和语义色板：

- **低风险**：灰蓝 / 青灰方向；
- **轻度风险**：雾蓝 / 浅琥珀方向；
- **中度风险**：柔和琥珀 / 土黄方向；
- **高风险**：克制红棕 / 暗玫瑰棕方向。

安全风险提示可以使用浅红棕底、红棕边框或深红棕文字，但禁止大面积高饱和红色、警报式视觉、闪烁提示和恐吓式设计。风险颜色不得成为唯一信息来源，必须配合文字、标题、说明或图表标签。

### Named Rules

**品牌色与风险色分离规则。** 主色不表达风险等级，风险色不承担品牌按钮职责。

**绿色克制规则。** 绿色不得作为全站主色、主 CTA 色或结果核心表达色，只能作为轻量环境辅助色。

**无焦虑营销规则。** 禁止高饱和红、霓虹渐变、大面积金色、纯蓝法律模板、紫蓝 AI 渐变和过多彩色标签。

## 3. Typography

字体策略不必局限于默认系统字体，但必须优先保证中文可读性、加载稳定性、版权安全、跨设备一致性、大陆访问稳定性，以及与 PNG / PDF 导出的兼容。

**Display Font:** 待确认，可由 Impeccable 后续评估克制中文衬线标题或高质量现代无衬线方案。  
**Body Font:** 优先使用稳定中文系统字体或已确认授权、可本地部署的清晰无衬线字体。  
**Label/Mono Font:** 无强制需求，表单和标签应优先可读。

**Character:** 字体应支持长文本阅读、结果解释和敏感信息表达。标题可以有适度专业质感，但正文必须清晰、稳定、不过度装饰。

### Hierarchy

- **Display**（600-700，桌面 32-44px，移动端 26-32px）：用于页面主标题、量表名称和结果页核心结论。
- **Headline**（600，20-24px）：用于结果区、后续支持区、安全提示区等主要模块标题。
- **Title**（600，17-20px）：用于卡片标题、维度标题、弹窗标题。
- **Body**（400，15-16px，line-height 1.65-1.85）：用于说明、结果解释、边界提示和表单辅助文案。长文本应控制行宽，避免桌面端过长。
- **Label**（500-600，12-14px）：用于表单 label、badge、错误提示和风险标签。错误提示可为 12-13px，但必须可读。

### Named Rules

**授权优先规则。** 不得引入未经确认授权的商业字体，不得依赖影响大陆访问稳定性的外部字体资源。若后续建议使用特定字体文件或第三方字体服务，必须先由用户确认字体来源、授权方式、加载方式和对 PNG / PDF 导出的影响。

**正文可读规则。** 不要让整页都是粗体卡片标题。层级通过字号、字重、行高、间距和位置共同建立。

**导出兼容规则。** 字体调整必须考虑 PNG / PDF 导出，不能只看浏览器页面效果。

## 4. Elevation

本系统使用轻量层级，而不是厚重阴影。高级感应来自细腻层级、稳定留白和清楚结构，不来自强浮层、玻璃拟态或装饰性阴影。

### Shadow Vocabulary

- **Base Surface**：常规卡片可使用低对比边框或极轻阴影，也可以不使用阴影。卡片应靠背景、边框和空间区分层级。
- **Interactive Lift**：按钮、可点击卡片或弹窗内动作可以有极轻 hover / focus 反馈，避免元素跳动或强浮起。
- **Modal Layer**：弹窗需要清楚层级，背景遮罩应克制、柔和，足以聚焦内容但不制造压迫。
- **Alert Layer**：安全风险提示视觉权重应高于普通说明卡，但低于警报页面。它可以使用浅红棕底、细边框或标题强调，不应依赖厚阴影。

### Named Rules

**轻层级规则。** 阴影要轻，遮罩要克制，弹窗层级清楚，不使用厚重阴影或强浮层。

**无默认玻璃拟态规则。** 不使用大量 blur、半透明玻璃卡片或浮夸科技感作为默认视觉语言。

**状态不位移规则。** hover、focus、active 状态不应造成布局位移或视觉抖动。

## 5. Components

### Cards

常规卡片圆角建议为 8-12px。弹窗、导出结果卡或特殊结果容器可在需要时使用 12-16px。卡片应使用低对比细边框、轻阴影或无阴影，内边距建议 20-28px。背景使用白色或近白色，但不应刺眼。

卡片用于承载明确的结果、表单、提示或图表模块。不要把每个信息块都做成独立广告卡，避免卡片过多导致碎片化。禁止卡片嵌套卡片。

### Buttons

按钮必须有主次层级。主按钮用于开始测评、提交信息、下载结果、确认弹窗。主按钮使用品牌界面主色，不使用风险等级色。次按钮用于重新测评、返回、关闭或次级动作。

禁用按钮应明确不可点击，但文字仍需可读。提交中应显示明确状态，例如“提交中...”，并禁止重复点击。提交成功后按钮变为“已提交”。

### Inputs and Selects

表单应清楚、安定、可恢复。label 必须明确，placeholder 克制，输入框高度一致，聚焦状态清楚。错误状态使用低饱和红，错误提示显示在输入框下方，不使用浏览器 alert。

服务意向下拉栏默认显示：

```text
请选择
```

正式选项为：

```text
需要法律支持
需要心理支持
仅需深度报告
```

默认“请选择”不应像真实选中项。服务意向必须中性呈现，不默认导向律所，也不默认导向心理支持机构。

### Modals

提交成功后根据服务意向显示不同弹窗。法律支持与心理支持弹窗应保持同等视觉权重。`deep_report` 弹窗显示小助理二维码，但不应显得低级、强制引流或像营销弹窗。

弹窗应居中，背景遮罩轻柔，内容简洁，按钮明确，二维码清晰。移动端弹窗不得超屏，关闭按钮必须明确。

### Radar Chart

雷达图是结构辅助，不是结果页唯一主视觉。它应保持中性，标签清晰，移动端可读，不使用过强颜色，也不喧宾夺主。风险颜色使用必须符合独立风险语义色板。

### Risk Alert

安全风险提示应明确、克制、严肃但不恐吓。视觉权重高于普通说明卡，低于警报页面。禁止使用闪烁、强红、大图标压迫或威胁式措辞。

高风险安全题触发时，即使总分未达到高风险，也应显示安全风险提示。

### Disclaimers

免责声明应清楚但不压住结果页主内容。建议放在页面底部，用较低视觉权重呈现，但保持可读。免责声明不应成为恐吓文本，也不应被隐藏到完全不可见。

### PNG Export Card

页面展示 UI 与 PNG 导出 UI 必须保持边界。首批 UI 优化不应修改 PNG 导出逻辑，不随意改变导出容器尺寸，不把页面滚动布局误用于导出图。如果优化导出图，应单独作为任务处理。

导出图应更像“简版测评结果卡”，不必完全等同页面视觉。字体、颜色、布局调整必须确认不会破坏 PNG / PDF 导出。

### Mobile Layout

移动端应改为单栏。结果页推荐顺序为：总结果、安全提示、雷达图、后续支持表单、免责声明。移动端要求选项按钮高度足够、输入框易点、下拉栏易操作、弹窗不超屏、二维码可见、长文本不拥挤、不出现横向滚动。

### State Rules

初始状态应清楚告诉用户下一步要做什么，不应页面一加载就显示红色错误。字段错误提示只在用户输入过、失焦后或尝试提交后显示。提交失败后表单仍应可编辑，允许用户再次提交。提交成功后下拉栏和输入框禁用，不可重复提交。

“与本人情况无关”应被解释为不计分，而不是错误。当有效作答过少时，应提示结果参考性有限，但不要否定用户体验。

## 6. Do's and Don'ts

### Do:

- **Do** 让页面像专业机构合作产品，而不是单一机构广告页。
- **Do** 把法律支持、心理支持和深度报告并列呈现，让用户主动选择服务意向。
- **Do** 使用暖白底、雾蓝灰 / 石墨蓝主色、暖灰褐辅助和独立风险语义色板。
- **Do** 将品牌界面色与风险语义色分开管理。
- **Do** 用留白、比例、排版、低噪音色彩和清楚层级体现成熟服务质感。
- **Do** 保持表单 label、错误提示、禁用状态、提交中状态和提交成功状态清楚可读。
- **Do** 在移动端保持单栏、可点、可读、无横向滚动。
- **Do** 保留 PNG 导出边界，首批 UI 优化不修改导出逻辑。
- **Do** 在完整 visual critique / Impeccable critique 前获取真实页面截图、localhost、本地浏览器或等价视觉证据。

### Don't:

- **Don't** 做成问卷星感：大量普通表单、粗边框、缺少视觉层级和可信感。
- **Don't** 做成娱乐测试感：类 MBTI、小红书人格测试、夸张标签、戏谑文案、人格类型娱乐化。
- **Don't** 做成律所强营销落地页：大面积律师背书、立即咨询、强转化按钮、过度法律蓝金视觉、单一律所引流导向。
- **Don't** 做成医疗诊断感：医院量表、心理疾病筛查、临床问卷或诊断报告。
- **Don't** 做成 AI 工具模板感：紫蓝渐变、大量玻璃拟态、浮夸科技感或泛 AI SaaS 风。
- **Don't** 做成恐吓式风险页：大面积红色、警报图标、威胁式措辞或制造焦虑。
- **Don't** 做成后台管理表单感：信息堆叠、灰白表格、缺乏引导、像内部系统而不是用户产品。
- **Don't** 用大面积金色、黑金商务、厚重阴影或豪华会所感表达高级感。
- **Don't** 把绿色作为全站主色、主 CTA 色或结果核心表达色。
- **Don't** 在没有真实页面证据时声称完成完整视觉审查。若只能基于代码与 CSS 做检查，必须明确标注为“代码层静态 UI 审查”，不得等同于完整视觉审查。
- **Don't** 在缺少截图时假装完成视觉审查。如果需要用户手动提供截图，Codex 必须先停下来列出截图清单。
