# 项目当前状态交接摘要（阶段更新版）

## 1. 文档用途

本文档用于在“离婚力量表网页端”项目进入下一轮开发前，快速交代当前稳定基线、已完成事项、当前未完成事项、下一阶段建议顺序与协作注意事项，供后续 GPT / Codex 新对话继续接手。

---

## 2. 项目当前总体状态

当前项目已完成两个阶段的核心收口：

### 2.1 公开前台主流程阶段

公开前台量表站点已完成并稳定运行，已实现：

- Next.js 前台主流程
- 系统分流题前置
- 普通题随机
- 自动下一题
- 动态满分计算
- 结果分级展示
- 短板维度识别
- 结果页页面内提交反馈
- 联系方式表单嵌入结果页
- `contact_name`、`contact_phone`、`contact_wechat` 与结果同次 `insert` 写入 Supabase
- 结果页动态维度雷达图（按子女 / 跨境分流动态显示 5 / 6 / 7 维）
- 雷达图简称统一为：心理、经济、亲权、财权、知法、情感、域外
- 结果页留资与提交成功承接文案优化
- 简洁版结果图导出（按钮文案为“导出结果”）
- GitHub Pages 免费部署已跑通，大陆网络可访问，提交结果可正常传入 Supabase

### 2.2 轻量后台第一轮阶段

围绕律所内部使用场景，项目已进入后台 MVP 路线，并已完成第一轮落地：

- 已明确后台第一版的范围、字段口径与页面顺序
- 已补齐 `assessment_results` 后台第一版所需核心字段
- 已完成独立后台子项目初始化：`apps/admin-mvp`
- 已完成后台最小鉴权第一轮接入
- 已完成后台列表页 MVP 第一轮
- 已完成本地手工验收与 `git push`

当前判断：

- 公开前台继续作为量表入口保留
- 后台不再尝试硬做进当前公开静态前台项目
- 后台改走“同仓库独立内部后台子项目”路线

---

## 3. 当前仓库结构状态

当前仓库采用“主项目前台 + 独立后台子项目”结构：

- 根目录主项目：继续承载公开量表前台
- `apps/admin-mvp/`：独立后台 MVP 子项目
- `docs/`：已纳入后台相关底座文档与阶段交接文档

当前判断：

- 这一结构方向已确认合理可行
- 后续后台功能继续在 `apps/admin-mvp/` 内推进
- 不再将后台能力继续塞入根目录前台项目

---

## 4. 当前 Supabase 状态

### 4.1 主表

后台 MVP 第一版继续以：

- `assessment_results`

作为线索主表。

### 4.2 已存在的前台写入字段

当前前台已稳定写入并在线使用的字段包括：

- `id`
- `created_at`
- `result_label`
- `answers`
- `total_score`
- `dynamic_full_score`
- `score_rate`
- `result_level`
- `child_gate_answer`
- `cross_border_gate_answer`
- `weaknesses`
- `submission_source`
- `contact_name`
- `contact_phone`
- `contact_wechat`
- `admin_note`
- `follow_up_status`

### 4.3 已补齐的后台 MVP 第一版字段

已在 Supabase 手动补齐以下字段：

- `dimension_scores`
- `service_type`
- `assigned_to`
- `appointment_owner`
- `appointment_time`
- `last_follow_up_at`
- `updated_by`
- `updated_at`
- `report_visibility`
- `report_version`
- `report_generated_at`

并已完成：

- 历史数据默认值回填
- `follow_up_status` 默认值设置
- `service_type` 默认值设置
- `report_visibility` 默认值设置
- `updated_at` 自动更新时间机制

### 4.4 当前字段口径重点

当前后台第一版需继续遵守：

- `service_type` 使用 `text[]`
- `service_type` 默认定位为 `none`
- `assigned_to` 与 `appointment_owner` 必须分开
- `report_visibility` 第一版默认按 `internal_only` 理解
- `dimension_scores` 字段已存在，但前台暂未接入写入

---

## 5. 后台底座文档状态

当前已形成并纳入项目来源 / 仓库 `docs/` 的后台底座文件包括：

- 《离婚力量表后台MVP范围与数据口径草案V1》
- 《离婚力量表后台MVP第一版字段清单V1》
- 《离婚力量表后台MVP第一版页面清单与开发顺序V1》
- 《项目当前状态交接摘要_阶段更新版》

这些文档已足够继续支撑下一轮后台开发协作。

---

## 6. 独立后台子项目当前完成情况

当前 `apps/admin-mvp` 已完成：

### 6.1 子项目初始化

- 独立 `package.json`
- 独立 `next.config.mjs`
- 独立 `app/`、`components/`、`lib/`
- 独立 `.env.example`
- 独立 README

### 6.2 最小鉴权第一轮

已实现：

- `/login` 登录页
- 登录后写入 HttpOnly cookie
- `/leads` 受保护路由
- 未登录访问 `/leads` 自动跳转 `/login`
- 退出登录后再次访问 `/leads` 会被重新拦回 `/login`

当前判断：

- 后台第一轮最小鉴权主链路已跑通
- 该方案当前可作为后台 MVP 第一轮内部测试门槛
- 但其仍属于最小实现，不等同于最终成熟权限体系

### 6.3 后台列表页第一轮

已实现：

- 后台线索列表页路由
- 默认列展示
- 基础读表
- 基础筛选
- 姓名 / 电话搜索
- 组合筛选
- 交叉检索
- “查看详情”占位入口

### 6.4 已完成的本地验收

已完成手工验证并通过：

- 登录页可打开
- 未登录访问 `/leads` 会跳回 `/login`
- 登录后可进入列表页
- 列表页可正常读取真实数据
- 姓名搜索正常
- 电话搜索正常
- 跟进状态筛选正常
- 结果等级筛选正常
- 负责人筛选正常
- 服务类型筛选正常
- 交叉检索正常
- 退出登录后会再次被拦回 `/login`
- “查看详情”当前仅为占位，不会误跳转到未完成详情页

### 6.5 当前刻意未实现

当前后台子项目第一轮尚未实现：

- 详情页主体
- 核心字段编辑
- CSV 导出
- 时间区间筛选
- 详细报告模块
- 批量操作
- 仪表盘 / 图表
- 复杂权限系统
- 完整 CRM 化能力

---

## 7. 当前阶段判断

当前项目可视为已经形成新的稳定基线：

### 稳定基线 A：公开前台

- 可继续正常作为量表入口使用
- 当前不再优先继续增强前台产品化细节

### 稳定基线 B：独立后台第一轮

- 后台子项目已成功启动
- 最小鉴权已跑通
- 列表页 MVP 第一轮已完成并完成本地验收

因此，当前阶段不再是：

- 是否开发后台
- 后台是否放在公开前台里
- 是否还依赖 Supabase Table Editor 作为长期正式方案

而已经明确进入：

**独立后台 MVP 的持续实现阶段。**

---

## 8. 下一阶段建议顺序

当前建议按以下顺序继续推进：

### 第一步：后台详情页

下一轮优先进入：

- 单条记录详情查看
- 核心字段编辑
- 保存更新
- `updated_at` / `updated_by` 联动

### 第二步：列表页增强

详情页收口后，再考虑：

- 时间区间筛选
- CSV 导出
- 轻分页 / 展示增强

### 第三步：详细报告与后续能力

在详情页与列表页主链路稳定后，再考虑：

- 详细报告模块
- 内部导出详细报告
- 后续更成熟的内部权限体系

---

## 9. 当前不建议做的事

当前阶段不建议：

- 再把后台能力往根目录公开前台项目里回塞
- 当前就扩成复杂 CRM
- 当前就做用户端报告开放
- 当前就做复杂角色权限树
- 当前就做后台仪表盘与图表
- 当前就再改 Supabase 主表结构，除非出现明确需求

---

## 10. 协作方法提醒

后续继续使用 GPT / Codex 协作时，仍建议保持以下习惯：

- 先由 GPT 判断任务，不直接大段输出代码
- 先判断是否需要新开 Codex 线程
- 每个稳定功能块收口后，下一功能块默认新开线程
- 凡涉及终端命令，必须同时解释命令含义与执行目的
- 先让 Codex 输出最小改动方案，再决定是否实施
- 当任务边界、实现路径、项目形态发生变化时，不在旧线程中硬接着写，应新开线程切断旧上下文污染

---

## 11. 当前一句话结论

**项目已从“公开量表前台阶段”正式进入“独立内部后台 MVP 持续实现阶段”；当前公开前台稳定，后台子项目第一轮已跑通，下一轮应优先进入后台详情页开发。**

